services:
  app:
    build: .
    ports:
      - "3010:3010"
    environment:
      - NODE_ENV=production
      - PORT=3010
      - DB_PATH=/app/data/db.sqlite
    volumes:
      - db-data:/app/data

volumes:
  db-data:
